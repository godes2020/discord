# Как использовать веб-редактор эмбедов

## Запуск

1. Запустите бота командой: `node bot.js`
2. Веб-редактор автоматически запустится на: **http://localhost:3000**
3. Откройте браузер и перейдите по адресу выше

## Функции веб-редактора

### 1. Выбор сервера и канала
- В левой панели выберите сервер Discord из списка
- После выбора сервера появится список его текстовых каналов
- Выберите канал, в который хотите отправить эмбед

### 2. Редактор кода
- В правой панели находится текстовый редактор для JSON кода
- Вставьте JSON код вашего эмбеда
- Поддерживается формат V2 Components из существующих файлов

### 3. Быстрые шаблоны
Доступны кнопки для быстрой загрузки шаблонов:
- **Заказ тикета** - шаблон кнопки создания тикета
- **Буст тикета** - шаблон кнопки заказа бустов
- **Закрытие заказа** - шаблон ЛС при закрытии тикета
- **Закрытие буста** - шаблон ЛС при закрытии буст-тикета

### 4. Отправка эмбеда
- Заполните все поля (сервер, канал, код)
- Нажмите кнопку "📤 Отправить эмбед"
- Статус отправки отобразится под кнопкой

## Формат JSON

Веб-редактор принимает тот же формат JSON, что используется в файлах:
- `ticket.json`
- `boost.json`
- `embed_*.json`

## Требования

- Бот должен быть запущен
- Бот должен иметь права на отправку сообщений в выбранном канале
- JSON код должен быть валидным

## Порт

По умолчанию веб-сервер запускается на порту **3000**.
Если нужно изменить порт, отредактируйте `PORT` в файле `webserver.js`.
